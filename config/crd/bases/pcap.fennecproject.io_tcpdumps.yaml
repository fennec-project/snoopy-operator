
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: tcpdumps.pcap.fennecproject.io
spec:
  group: pcap.fennecproject.io
  names:
    kind: Tcpdump
    listKind: TcpdumpList
    plural: tcpdumps
    singular: tcpdump
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tcpdump is the Schema for the tcpdumps API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TcpdumpSpec defines the desired state of Tcpdump
            properties:
              fileSize:
                description: Equivalent to tcpdump -C (upper case) parameter
                format: int64
                type: integer
              interfaceName:
                description: Network virtual interface or link name on the pod to
                  run tcpdump on
                type: string
              name:
                description: tcpdump profile name to be applied to a set of pods
                type: string
              packetCount:
                description: Equivalent to tcpdump -c parameter
                format: int64
                type: integer
              pcapFilePath:
                description: Equivalent to -w tcpdump parameter
                type: string
              podLabel:
                additionalProperties:
                  type: string
                description: Label to identify the target pods
                type: object
              targetNamespace:
                description: Namespace where the target pods live
                type: string
            type: object
          status:
            description: TcpdumpStatus defines the observed state of Tcpdump
            properties:
              endTime:
                type: string
              pcapFilePath:
                type: string
              startTime:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
